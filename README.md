# readinessandliveness

This is a simples readiness and liveness pock application with actuator and springboot.

Run ```docker build -t "app" -f Dockerfile .``` to build a docker image with application.
